<div class="d-flex flex-column align-items-center">


<h1><%= t("common.app_title") %></h1>
<h2><%= t('.sign_in') %></h2>

<div class="text-bg-light fs-5 p-4 my-4">
  本アプリは、複数の拠点を持つ企業様に向けて、<br>
  <strong>従業員の通勤によるCO2排出量</strong>の<br>
  簡易な算定・集計をサポートいたします。
</div>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field mb-3">
    <%= f.label :email, class: "form-label" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control w-auto" %>
  </div>

  <div class="field">
    <%= f.label :password, class: "form-label" %><br />
    <%= f.password_field :password, autocomplete: "current-password", class: "form-control w-auto" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions text-center my-4">
    <%= f.submit t('.sign_in'), id: "sign-in", class: "btn btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

<div class="d-flex">
  <%= link_to "ゲストログイン（一般）", users_guest_sign_in_path, method: :post, class: "btn btn-outline-dark" %>
  <%= link_to "ゲストログイン（管理者）", users_guest_admin_sign_in_path, method: :post, class: "btn btn-outline-dark ms-3" %>

</div>

</div>